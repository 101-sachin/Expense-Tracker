/**
 * Lets Metro resolve the bare specifier `@env`. Values come from `.env` at compile time
 * via `module:react-native-dotenv`, which replaces `import … from '@env'` in app code.
 */
module.exports = {
  BASE_URL: '',
};
